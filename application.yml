# Shared configuration applied to every client of the Config Server.
# Service-specific files (e.g. song-service.yml) override these values.
management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh,busrefresh,circuitbreakers,circuitbreakerevents
  endpoint:
    health:
      show-details: when-authorized
